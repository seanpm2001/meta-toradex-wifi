diff --git a/Makefile b/Makefile
index d0322ab..e06a796 100755
--- a/Makefile
+++ b/Makefile
@@ -640,6 +640,9 @@ else
 default:
 	$(MAKE) -C $(KERNEL_SRC) M=$(PWD) ARCH=$(ARCH) CROSS_COMPILE=$(CROSS_COMPILE) modules
 
+modules_install:
+	$(MAKE) -C $(KERNEL_SRC) M=$(PWD) INSTALL_MOD_PATH=$(MODLIB) modules_install
+
 endif
 
 ###############################################################
